# regex-playground

Live regex tester — single HTML file, no dependencies, works offline.

Open `index.html` in any browser.

**Features:**
- Real-time highlighting as you type
- Match list with positions and capture groups
- Quick reference panel — click any token to insert it
- Dark / light mode
